app = "mr-rajpoot-studio-worker"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[mounts]
  source = "media_cache"
  destination = "/data/media_cache"

[env]
  MEDIA_CACHE_DIR = "/data/media_cache"
  MAX_CONCURRENT_STREAMS = "2"
  NODE_ENV = "production"
